language: java

sudo: false
addons:
  apt:
    packages:
      - oracle-java8-installer
      
jdk:
  - oraclejdk8
  
service:
  - mysql

before_script:
  - mysql -u guestbook -p $pass -e 'create database guestbook;'

script:
        - cd $TRAVIS_BUILD_DIR/guestbook
        - mvn test

notifications:
  email: false
